// client/js/views/stats.js — vue Stats (squelette, contenu à construire)

/**
 * @param {HTMLElement} container
 */
function renderStats(container) {
  container.innerHTML = '<p>Vue Stats (à construire)</p>';
}

export { renderStats };
